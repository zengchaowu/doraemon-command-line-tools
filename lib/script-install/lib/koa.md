# 创建ggkoa项目
## 需要安装node
```
mkdir project_koa
cd project_koa
npm init -y
npm i koa --save
npm i koa-router --save
npm i dotenv --save

git init
echo "node_modules/" > .gitignore
git add .
git commit -m "init"
```