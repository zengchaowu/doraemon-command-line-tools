---
title: 变量
slug: /overview/variable
---

变量名有确定的属性和意义

## Dart
任何保存在变量中的都是对象。
强类型语言


## 标识符与变量名的区别

标识符只是一个由特定规则限制的字符串。本身没有意义。

但是标识符可以用来标识某个数据或者对象，此时，这个标识符就是变量名。

标识符与对象进行关联就是绑定。标识符在进行绑定以后，就是一个变量名。

发生在编译过程中的绑定称为静态绑定，发生在程序运行过程中的绑定称为动态绑定。
C++虚函数为动态绑定。

## 变量名包含名字的意义和属性
